'use strict';

/**
 * maintenance-request router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maintenance-request.maintenance-request');